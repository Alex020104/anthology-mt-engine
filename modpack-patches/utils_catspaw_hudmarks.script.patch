--- a/gamedata/scripts/utils_catspaw_hudmarks.script
+++ b/gamedata/scripts/utils_catspaw_hudmarks.script
@@
 function HUDMarkerManager:__init()
     dl("[HUDMarkMgr] Initializing HUD Marker Manager")
     self.init_time          = time_global()
-    self.update_int         = 15
+    -- 30 Hz is enough for marker placement/hover while avoiding a full walk of
+    -- every managed marker on nearly every rendered frame.
+    self.update_int         = 33
@@
 function HUDMarkerManager:actor_on_update()
     --vl("[HUDMarkerManager:actor_on_update]")
+    local now = time_global()
+    if (self.next_update or 0) > now then return end
+    self.next_update = now + (self.update_int or 33)
+
     local obj = level.get_target_obj()
@@
-    if (self.next_update or 0) > time_global() then return end
-    self.next_update        = time_global() + (self.update_int or 15)
     self:Update()
 end
