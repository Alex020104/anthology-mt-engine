# Anthology Performance v104 — RAK Indoor Ray Gate

Отдельный MO2-патч для `R.A.K Weapon Pack Adaptation`.

- Исправляет ошибку чтения `tail_enable_for_non`: раньше проверялась таблица настройки вместо её значения, поэтому предусмотренный ранний выход не работал.
- Сначала соблюдает выбранный интервал 400 мс и только затем проверяет оружие и запускает геометрические лучи.
- Один акустический замер использует одну позицию актёра вместо повторного Lua/C++-запроса для каждого направления.
- Повторно использует таблицы результатов и не создаёт временный список направлений при каждом замере.
- Число направлений, отражений, длины лучей, MCM и итоговый звук не изменены.
- Сохранения не меняются, новая игра не нужна.

Проверка: в логе должна быть строка `[anthology/v104/rak]`. Откат — отключить только этот аддон в MO2.
